[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "plotpilot-autopilot"
version = "1.0.0"
description = "Durable Host-composed PlotPilot Autopilot DAG runtime"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]