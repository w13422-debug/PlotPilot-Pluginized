{
  "$id": "https://plotpilot.local/contracts/plugin-ui-intent-v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "action_id": {
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
      "type": "string"
    },
    "capability_id": {
      "anyOf": [
        {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "event_type": {
      "enum": [
        "change",
        "click",
        "select_row",
        "change_tab",
        "select_node",
        "open_core_operation"
      ]
    },
    "freshness": {
      "additionalProperties": false,
      "properties": {
        "generation_id": {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
          "type": "string"
        },
        "plan_revision_id": {
          "anyOf": [
            {
              "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "plugin_release_id": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "workspace_id": {
          "anyOf": [
            {
              "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "workspace_revision_id": {
          "anyOf": [
            {
              "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "generation_id",
        "plugin_release_id",
        "workspace_id",
        "workspace_revision_id",
        "plan_revision_id"
      ],
      "type": "object"
    },
    "intent_id": {
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
      "type": "string"
    },
    "intent_kind": {
      "enum": [
        "invoke_capability",
        "open_core_operation",
        "request_candidate_preview",
        "request_job_cancel",
        "set_view_state"
      ]
    },
    "intent_seq": {
      "minimum": 1,
      "type": "integer"
    },
    "operation_key": {
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
      "type": "string"
    },
    "payload_asset_id": {
      "anyOf": [
        {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "render_seq": {
      "minimum": 1,
      "type": "integer"
    },
    "schema": {
      "const": "plugin-ui-intent/v1"
    }
  },
  "required": [
    "schema",
    "intent_id",
    "intent_seq",
    "render_seq",
    "action_id",
    "event_type",
    "intent_kind",
    "capability_id",
    "payload_asset_id",
    "operation_key",
    "freshness"
  ],
  "title": "PlotPilot plugin-ui-intent-v1",
  "type": "object"
}
